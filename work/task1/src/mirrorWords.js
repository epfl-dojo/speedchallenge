/**
 * ICTSkills 2020 — Trade 17 — Web Technologies
 *
 * Speed challenge — Task 1 — Mirror letters in words
 */

/**
 * Given some sentence, revert the order of the letters in each word.
 * The order of the words should stay the same.
 *
 * For instance, Hello World! should become olleH !dlroW.
 *
 * @param {String} sentence
 * @return {String}
 */
function mirrorWords(sentence) {
    // @TODO
}

module.exports = mirrorWords;
