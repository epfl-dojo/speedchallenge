<?php
/**
 * ICTSkills 2020 — Trade 17 — Web Technologies
 *
 * Speed challenge — Task 3 — String info
 */

/**
 * Given some string, return its length and its word count.
 *
 * A word is every combination of characters separated by a space, tabulation or a line return.
 * For instance, “can't” and “world-famous” are both counted as one word.
 *
 * @param string $str The input string
 * @return array An array containing a “length” and a “words” properties.
 *               For example: stringInfo('Hello world') should return
 *               ['length' => 11, 'words' => 2]
 */
function stringInfo(string $str): array {
    // @TODO
}
