/**
 * ICTSkills 2020 — Trade 17 — Web Technologies
 *
 * Speed challenge — Task 2 — Diagonal Sums
 */

/**
 * Write a JavaScript function that given a 2-dimensional (squared) array
 * of numbers finds the sum of both diagonals. The number in the center
 * is included in both diagonals.
 *
 * [
 *   [ 1, 2, 3 ],
 *   [ 4, 5, 6 ],
 *   [ 7, 8, 9 ]
 * ]
 *
 * Result = 1 + 5 + 9 + 3 + 5 + 7 = 30
 *
 * @param {Array<Array<number>>} arr
 * @return {number}
 */
function diagonalSums(arr) {
    // @TODO
}

module.exports = diagonalSums;
